import React, { useEffect, useRef, useState } from "react";

interface OtpProps {
    length: number;
    onSubmit: (otp: string) => void;
}



const Otp: React.FC<OtpProps> = ({ length, onSubmit }) => {
    return <>

    </>
};

export default Otp;
